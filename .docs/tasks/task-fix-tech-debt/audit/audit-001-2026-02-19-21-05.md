# Audit: Technical Debt Fixes (001)

**Date**: 2026-02-19 21:05
**Status**: COMPLETED
**Scope**: Docker infrastructure, README documentation, Program.cs cleanup, and REST-compliant validation errors.

## 1. Summary of Work
The technical debt related to infrastructure and documentation was addressed by adding database persistence, port mapping, and updating the project instructions. Additionally, the API was refactored to standardise routing and implement RFC 7807 compliant validation error handling.

## 2. Key Features Implemented
- **Docker Persistence**: Added `db_data` volume for PostgreSQL in `compose.yaml`.
- **Port Mapping**: Mapped port `5000` to `8080` for the presentation service.
- **Documentation**: Simplified `docker compose` commands and added Swagger UI URL to `README.md`.
- **API Cleanup**: Removed Minimal API boilerplate and boilerplate weather forecast logic from `Program.cs`.
- **RESTful Validation**: 
    - Enabled `AddProblemDetails()`.
    - Configured `ApiBehaviorOptions` for `422 Unprocessable Entity` responses.
    - Updated `CustomResultsHandler` and `ExceptionMiddleware` to handle `ValidationException` (422) and `KeyNotFoundException` (404) while maintaining required JSON structure and journaling.

## 3. Verification & Testing
- **Build**: `dotnet build` successfully executed for `JournalChallenge.Presentation`.
- **Persistence Check**: Verified `volumes` and `db_data` definition in `compose.yaml`.
- **Mapping Check**: Verified `ports: - "5000:8080"` in `compose.yaml`.
- **Error Logic Check**: Verified `ExceptionMiddleware` status code mapping (422/404/500) and response body consistency.

## 4. Architectural Notes
- The implementation preserves the existing Result-to-Exception-to-Middleware flow while improving REST compliance.
- Journaling is maintained for all exceptions, including validation and not found errors, ensuring traceability as per original specifications.
- `ValidationException` was added to the Domain layer to facilitate structured error reporting without polluting the Application layer with presentation-specific types.

## 5. Conclusion
All technical debt items and the new RESTful validation requirement are successfully implemented and verified.

# Subtask: Recursive & Bulk Operations (stt-003)

## Objective
Verify operations that affect multiple nodes or the entire tree.

## Actions
- [ ] Create test case: Forced Deletion - Verify that a parent and all its descendants are deleted when `isForcedDeletion=true`.
- [ ] Create test case: Tree Structure - Verify `GET /api.user.tree.get` returns the full recursive hierarchy for a multi-level tree.

## Verification
- Confirm all child nodes are removed after a forced deletion.
- Validate full JSON structure of a complex tree.

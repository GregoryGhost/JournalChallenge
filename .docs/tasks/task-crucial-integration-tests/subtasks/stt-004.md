# Subtask: Exception Journaling & Audit End-to-End (stt-004)

## Objective
Confirm that all exceptions are correctly logged and retrievable via the Journal API.

## Actions
- [x] Create test case: Journal Linkage - Trigger a `SecureException`, capture its ID, and retrieve the full details using `GET /api.user.journal.getSingle`.
- [x] Create test case: Data Integrity - Verify the journal entry contains the stack trace and original request parameters.
- [x] Create test case: Search & Pagination - Verify `POST /api.user.journal.getRange` correctly handles `skip`, `take`, and `search` filters.

## Verification
- Confirm the journal data matches the triggered error details.
- Confirm pagination and filtering work as expected.

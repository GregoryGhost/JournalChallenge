# Subtask: Tree & Node Lifecycle Verification (stt-001)

## Objective
Verify the basic operations of creating, retrieving, and renaming nodes and trees.

## Actions
- [x] Create test case: `GET /api.user.tree.get` - Verify automatic tree creation when a new name is provided.
- [x] Create test case: `POST /api.user.tree.node.create` - Verify creation of root and nested child nodes.
- [x] Create test case: `POST /api.user.tree.node.rename` - Verify a node's name is updated correctly.

## Verification
- Run tests and confirm success responses.
- Verify tree structure is correct after each operation.

# stt-003: End-to-End Integration Testing

## Objective
Verify the entire system flow using the `.http` file.

## Tasks
- [ ] Update `.http` file with all tree/node endpoints.
- [ ] Verify success scenarios for all operations.
- [ ] Verify `SecureException` triggers when deleting node with children (unforced).
- [ ] Verify validation errors are handled correctly.
- [ ] Check `ExceptionJournal` for correct logging of these events.

# stt-001: Tree Controller: Retrieval & Auto-creation

## Objective
Implement the endpoint to retrieve the entire tree by name.

## Tasks
- [ ] Create `TreeController.cs` in the Presentation project.
- [ ] Implement `POST /api.user.tree.get`.
- [ ] Map `treeName` from Query.
- [ ] Use `IRestCustomResultsHandler` to handle the `Result` from `IGetTreeQueryHandler`.

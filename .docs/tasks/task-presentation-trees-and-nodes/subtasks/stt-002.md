# stt-002: Node Controller: Create, Rename, Delete

## Objective
Implement the endpoints for node-level operations.

## Tasks
- [ ] Create `NodeController.cs` in the Presentation project.
- [ ] Implement `POST /api.user.tree.node.create` (Map `treeName`, `parentNodeId`, `nodeName`).
- [ ] Implement `POST /api.user.tree.node.rename` (Map `nodeId`, `newNodeName`).
- [ ] Implement `POST /api.user.tree.node.delete` (Map `nodeId`, `isForcedDeletion`).
- [ ] Use `IRestCustomResultsHandler` for all endpoints.

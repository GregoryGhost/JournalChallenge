# stt-002: Node Controller: Create, Rename, Delete

## Objective
Implement the endpoints for node-level operations.

## Tasks
- [x] Create `NodeController.cs` in the Presentation project.
- [x] Implement `POST /api.user.tree.node.create` (Map `treeName`, `parentNodeId`, `nodeName`).
- [x] Implement `POST /api.user.tree.node.rename` (Map `nodeId`, `newNodeName`).
- [x] Implement `POST /api.user.tree.node.delete` (Map `nodeId`, `isForcedDeletion`).
- [x] Use `IRestCustomResultsHandler` for all endpoints.

## Status: COMPLETED (2026-02-19)

# stt-005: Feature: Get Journal Range

## Objective
Implement the Application Layer logic to retrieve paginated journal entries.

## Tasks
- [x] Create `GetJournalRangeQuery` and `JournalEntryDto`.
- [x] Implement `GetJournalRangeQueryHandler` with pagination (`skip`/`take`) and filtering (`from`, `to`, `search`).
- [x] Implement `GetJournalRangeQueryValidator`.
- [x] Add unit tests in `JournalChallenge.Application.Tests/Journal/GetRange/`.

## Status: COMPLETED (2026-02-19)

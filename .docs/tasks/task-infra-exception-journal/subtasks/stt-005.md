# stt-005: Feature: Get Journal Range

## Objective
Implement the Application Layer logic to retrieve paginated journal entries.

## Tasks
- [ ] Create `GetJournalRangeQuery` and `JournalEntryDto`.
- [ ] Implement `GetJournalRangeQueryHandler` with pagination (`skip`/`take`) and filtering (`from`, `to`, `search`).
- [ ] Implement `GetJournalRangeQueryValidator`.
- [ ] Add unit tests in `JournalChallenge.Application.Tests/Journal/GetRange/`.

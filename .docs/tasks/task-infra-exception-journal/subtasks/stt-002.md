# stt-002: Custom Result Mapping

## Objective
Implement the logic to map Application `Result` failures to specific exceptions required by the presentation layer.

## Tasks
- [x] Implement `CustomResultsHandler` in `JournalChallenge.Infrastructure.Core/Implementations/`.
- [x] Map `ErrorType.Secure` to a new `SecureException`.
- [x] Map all other `ErrorType` values to a generic `Exception` with the domain error message.
- [x] Register the handler in Dependency Injection.

## Status: COMPLETED (2026-02-19)

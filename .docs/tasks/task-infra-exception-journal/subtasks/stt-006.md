# stt-006: Feature: Get Single Journal Entry

## Objective
Implement the Application Layer logic to retrieve a detailed journal entry.

## Tasks
- [ ] Create `GetJournalSingleQuery` and `JournalDetailDto`.
- [ ] Implement `GetJournalSingleQueryHandler` to fetch by `Id`.
- [ ] Implement `GetJournalSingleQueryValidator`.
- [ ] Add unit tests in `JournalChallenge.Application.Tests/Journal/GetSingle/`.

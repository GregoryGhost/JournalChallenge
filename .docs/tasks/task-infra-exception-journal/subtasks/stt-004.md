# stt-004: Middleware: Persistence & Response Formatting

## Objective
Complete the middleware by logging details to the DB and formatting the HTTP response.

## Tasks
- [x] Inject `IApplicationDbContext` into the middleware (or use a factory).
- [x] Persist all captured details (ID, Type, Message, StackTrace, Params) to the `ExceptionJournal` table.
- [x] Implement response formatting logic:
    - `SecureException` -> `{"type": "Secure", "id": "...", "data": {"message": "..."}}`.
    - Other exceptions -> `{"type": "Exception", "id": "...", "data": {"message": "Internal server error ID = ..."}}`.
- [x] Ensure HTTP status code is always 500.

## Status: COMPLETED (2026-02-19)

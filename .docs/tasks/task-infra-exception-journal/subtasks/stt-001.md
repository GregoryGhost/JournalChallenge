# stt-001: Entity & Database Audit

## Objective
Verify and finalize the `ExceptionJournal` domain entity and its infrastructure configuration.

## Tasks
- [ ] Audit `JournalChallenge.Domain/Journal/ExceptionJournal.cs` for correct types (`long Id`, `long EventId`).
- [ ] Ensure `Infrastructure/Journal/ExceptionJournalConfiguration.cs` uses `jsonb` for `QueryParams` and `BodyParams`.
- [ ] Verify database schema matches these requirements.

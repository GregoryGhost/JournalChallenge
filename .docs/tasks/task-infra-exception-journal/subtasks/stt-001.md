# stt-001: Entity & Database Audit

## Objective
Verify and finalize the `ExceptionJournal` domain entity and its infrastructure configuration.

## Tasks
- [x] Audit `JournalChallenge.Domain/Journal/ExceptionJournal.cs` for correct types (`long Id`, `long EventId`).
- [x] Ensure `Infrastructure/Journal/ExceptionJournalConfiguration.cs` uses `jsonb` for `QueryParams` and `BodyParams`.
- [x] Verify database schema matches these requirements.

## Status: COMPLETED (2026-02-19)
